{
  "name": "jump-to",
  "version": "1.0.0",
  "description": "A Vimium inspired keyboard shortcut script for browsers. Highlights all clickable elements on a page, then provides shortcuts to click them so you never have to leave your keyboard again!",
  "main": "main.js",
  "scripts": {
    "test": "tsc --noEmit && npx prettier --check src && npx playwright test",
    "build-package": "mkdir -p dist && cp -r src dist/ && cp manifest.json dist/"
  },
  "author": "",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/chrome": "^0.0.263",
    "@types/node": "^20.11.28",
    "prettier": "^3.2.5",
    "prettier-plugin-jsdoc": "^1.3.0",
    "typescript": "^5.4.2"
  }
}
